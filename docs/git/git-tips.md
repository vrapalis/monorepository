# Git Tips

## Git commit message
### Only include to commit related parts
``git add -p fileName``

## Interactive rabase
### git reabse -i HEAD~n
``fixup add to privouse commit, reword only change commit massage``
