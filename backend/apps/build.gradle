subprojects {
    subprojects {
        apply plugin: 'application.build'
        apply from: "${rootDir}/gradle/docker/docker.gradle"
    }

    apply from: "${rootDir}/gradle/docker/docker-compose.gradle"
}