spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: estate-management-config-server-service
    consul:
      config:
        enabled: false
      host: localhost
      port: 8500