group "${group}.services"

subprojects {
    ext {
        DOCKER_IMAGE_PREFIX = "${parent.parent.name}/${parent.name}"
    }

    apply plugin: 'de.delloit.www.backend.plugins.spring-boot-application'
    apply plugin: 'de.delloit.www.backend.plugins.docker'
}