spring:
  application:
    name: estate-management-authroziation-service
  banner:
    location: classpath:/banner/banner.txt
server:
  port: 8081


# Local Profile
---
spring:
  config:
    activate:
      on-profile: local
    import:
      - classpath:config/local/application-local.yml
