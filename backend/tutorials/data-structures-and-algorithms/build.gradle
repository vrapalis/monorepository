application {
    if (project.hasProperty('mainClass')) {
        mainClass = project.mainClass
    } else {
        mainClass = "com.vrapalis.www.tutorials.datastructuresandalgorithms.RunDataStructuresAndAlgorithms"
    }
}