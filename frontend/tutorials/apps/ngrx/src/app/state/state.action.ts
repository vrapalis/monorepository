import {createAction} from "@ngrx/store";

export const INCREASE_COUNTER_ACTION = createAction('INCREASE COUNTER ACTION');
export const DECREASE_COUNTER_ACTION = createAction('DECREASE COUNTER ACTION');
export const RESET_COUNTER_ACTION = createAction('RESET COUNTER ACTION');
