import {
  ArgsTable,
  Canvas,
  Meta,
  Story
} from '@storybook/addon-docs/blocks';
import { ButtonComponent } from './button.component';
import { MatButtonModule } from '@angular/material/button'; import { moduleMetadata } from '@storybook/angular'; import {
  TemplateAccent,
  TemplateBasic,
  templateButtonSourceBasic,
  templateButtonSourcePrimary,
  templateButtonSourceSourceAccent, templateButtonSourceSourceWarn,
  TemplatePrimary, TemplateWarn
} from './button.component.stories';

<Meta title="Components/Buttons/Standard Button" component={ButtonComponent}
      decorators={[
        moduleMetadata({ 'imports': [MatButtonModule], declarations: [ButtonComponent] })
      ]}
      argTypes={{
        type: {
          description: 'Change button type',
          table: {
            defaultValue: {
              summary: 'basic'
            },
            type: {
              //summary: 'Shows options to the Badge',
              //detail: 'Listing of available options'
            }
          },
          control: {
            type: 'select',
            options: ['basic', 'primary', 'accent', 'warn']
          }
        },
        disabled: {
          description: 'Disable button',
          table: {
            defaultValue: {
              summary: false
            },
            type: {
              //summary: 'Shows options to the Badge',
              //detail: 'Listing of available options'
            }
          },
          control: {
            type: 'select',
            options: [false, true]
          }
        },
        buttonType: {
          table: {
            disable: true
          }
        },
        ngOnChanges: {
          table: {
            disable: true
          }
        }
      }} />

# Standard Buttons
To use standard button import it in the module/component like
``` import: [EnButtonModule] ```
## Basic
<Canvas>
  <Story name="Basic" args={{ type: 'basic', disabled: false }} parameters={{
    docs: {
      source: {
        code: templateButtonSourceBasic,
      }
    }
  }}>
    {TemplateBasic.bind({})}
  </Story>
</Canvas>

## Primary
<Canvas>
  <Story name="Primary" args={{ type: 'primary', disabled: false }} parameters={{
    docs: {
      source: {
        code: templateButtonSourcePrimary,
      }
    }
  }}>
    {TemplatePrimary.bind({})}
  </Story>
</Canvas>

## Accent
<Canvas>
  <Story name="Accent" args={{ type: 'accent', disabled: false }} parameters={{
    docs: {
      source: {
        code: templateButtonSourceSourceAccent,
      }
    }
  }}>
    {TemplateAccent.bind({})}
  </Story>
</Canvas>

## Warn
<Canvas>
  <Story name="Warn" args={{ type: 'warn', disabled: false }} parameters={{
    docs: {
      source: {
        code: templateButtonSourceSourceWarn,
      }
    }
  }}>
    {TemplateWarn.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

